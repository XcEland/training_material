# SQL Server connection
DB_DRIVER=ODBC Driver 18 for SQL Server
DB_SERVER=localhost,1433
DB_NAME=TrainingDB
DB_USER=sa
DB_PASSWORD=StrongPassw0rd!2026
DB_TRUSTED=no

# Optional API authentication example.
# The World Bank sample API used in this lab does not require a key,
# but real APIs often require one.
EXTERNAL_API_KEY=
EXTERNAL_API_USER_AGENT=Trainingcred-Module7-Lab/1.0

# Rate limiting
API_MIN_SECONDS_BETWEEN_CALLS=1.0
